@model IEnumerable<PharmacyManagement.Web.Models.Customer>
@{
    ViewData["Title"] = "Customers";
}

<h1>Customers</h1>
<p>View, search, add, edit and delete customers.</p>

<form method="get" class="search-form">
    <input type="text" name="search" value="@ViewBag.Search" placeholder="Search by name, phone or email" />
    <button type="submit">Search</button>
    <a asp-action="Index">Clear</a>
    <a asp-action="Create" class="button-link">Add Customer</a>
</form>

<div class="table-wrap">
    <table>
        <thead>
            <tr>
                <th>Name</th>
                <th>Phone</th>
                <th>Email</th>
                <th>Address</th>
                <th></th>
            </tr>
        </thead>
        <tbody>
        @if (!Model.Any())
        {
            <tr>
                <td colspan="5">No customers found.</td>
            </tr>
        }
        else
        {
            foreach (var customer in Model)
            {
                <tr>
                    <td>@customer.Name</td>
                    <td>@customer.Phone</td>
                    <td>@customer.Email</td>
                    <td>@customer.Address</td>
                    <td class="actions">
                        <a asp-action="Details" asp-route-id="@customer.CustomerId">Details</a>
                        <a asp-action="Edit" asp-route-id="@customer.CustomerId">Edit</a>
                        <a asp-action="Delete" asp-route-id="@customer.CustomerId">Delete</a>
                    </td>
                </tr>
            }
        }
        </tbody>
    </table>
</div>
